import { ModelosComponent } from './../modelos/modelos.component';

export class Model {
    id: number;
    controller_id: number;
    name: string;
    nro_inputs: number;
    nro_outputs: number;

    constructor() {
        // this.menu = new Menu();
        // this.template = new Catalogue();
        // this.section = new Section();

    }
}
